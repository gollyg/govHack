# Add your team_id to the line
name={team_id}-file-sink-test

# The file below is mounted to the local file system. You can change this if you want.
file=/opt/docker/connect/output/test.sink.txt

# You don't have to change anything below this line
connector.class=FileStreamSink
tasks.max=1
topics=govhack-covid_cases_postcode

